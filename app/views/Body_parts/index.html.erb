<h1>These are the body parts for the exercises</h1>

<table class="table table-striped">
  <thead>
    <tr>
      <th><%= "name"%></th>
      <!--<th></th>-->
      <th></th>
      <th></th>
    </tr>
  </thead>

  <tbody>
    <% @body_parts.each do |body_part| %>
      <tr>
        <td><%= link_to body_part.name, body_part %></a></td>
        <!--<td><%= link_to 'Show', body_part %></td>-->
        <td><%= link_to 'Edit', edit_body_part_path(body_part) %></td>
        <td><%= link_to 'Delete', body_part, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<p> Add a new body part that you want to work out:</p>

<%= button_to 'New body part', new_body_part_path, :class => "btn-success", :method => :get %>
