<%= form_for(@circuit) do |f| %>
  <% if @circuit.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@circuit.errors.count, "error") %> prohibited this circuit from being saved:</h2>

      <ul>
      <% @circuit.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

<div>
  <table class="table table-striped">

    <thead>
    <tr><% @body_parts.each do |bp| %>
      <td> <%= bp.name  %></td>
      <% end %>
    </tr>
    </thead>

    <tbody>
      <tr>
        <% @body_parts.each do |bp| %>
      <td><%=   f.collection_check_boxes :exercise_ids, bp.exercises, :id, :name%>
                
       </td>
      <% end %>
    </tr>
    </tbody>

  </table>

</div>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name, :autofocus => true %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>


<!--.collection_check_boxes, :exercise_ids, Exercise.all, :id, :name-->